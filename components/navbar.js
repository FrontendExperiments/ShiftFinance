import {Box} from "framer-motion";

export default function Navbar({children}) {
    return (
        <nav>
            <Box w={"100%"}>
                Navigation Bar
            </Box>
        </nav>
    )
}